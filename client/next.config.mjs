/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['link.papareact.com', 'lighthouse.chotot.com','cdn.chotot.com', 'static.chotot.com'],
  
    },
};

export default nextConfig;

  
